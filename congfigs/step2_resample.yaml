# ========================================
# Step2: K线重采样配置（极简版）
# 所有路径/格式/周期/source_mode 从 main_config.yaml 继承
# ========================================

# Step2 特有配置：时间范围过滤（可选）
date_range:
  start: null  # 例: "2024-01-01"
  end: null    # 例: "2024-12-31"

# 重采样策略（可选）
resample:
  boundary: "right"     # 收盘价对齐右边界
  label: "right"        # 时间标签使用右边界
